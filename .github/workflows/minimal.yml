name: 2d action matrix

on: [push]

jobs:
  mjob:
    strategy:
      matrix:
        fruit: [apple, pear]
        animal: [cat, dog]
        include:
          - color: green
          - color: pink
            animal: cat
          - fruit: apple
            shape: circle
          - fruit: banana
          - fruit: banana
            animal: cat
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo hello world
