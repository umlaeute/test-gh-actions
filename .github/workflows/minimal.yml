name: 2d action matrix

on: [push]

jobs:
  mjob:
    strategy:
      fail-fast: false
      matrix:
        character: [A, B]
        number: [42, 666]
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo hello world
